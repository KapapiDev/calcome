name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Apply Prettier output
        run: npx prettier --write src/features/jeonse-loan-interest/components/jeonse-loan-interest-calculator.tsx src/features/jeonse-loan-interest/components/jeonse-loan-interest-calculator.test.tsx

      - name: Upload formatted files
        uses: actions/upload-artifact@v4
        with:
          name: jeonse-loan-prettier-output
          path: |
            src/features/jeonse-loan-interest/components/jeonse-loan-interest-calculator.tsx
            src/features/jeonse-loan-interest/components/jeonse-loan-interest-calculator.test.tsx
