# REG-EXP-001 — Expansion Regression Gate

Verified: 2026-08-27

## Baseline

- `main`: `0c37394e09f2b4f764c27d3b4b0a3d0cb26abc70` (P-061)
- Public calculators: 72
- Expansion progress: 21 / 49 complete, 28 remaining
- Next calculator after this gate: P-064 Jeonse Loan Limit Calculator

## Repository and CI regression

The latest calculator head for PR #290 (`77759652012f149179241b350552d47a955f4516`) passed exact-head GitHub Actions CI #1006. The quality job completed successfully for:

- full dependency audit reporting
- production high-severity dependency audit gate
- lint
- typecheck
- format verification
- all tests
- production build

The merged `main` commit is the corresponding squash merge of that validated head. No open Pull Request existed at the start of this gate, so no unfinished implementation was bypassed.

The existing full test/build gate is treated as the repository-side regression evidence for public calculator registration, locale routing, sitemap/indexability integration, metadata/structured-data behavior, and other repository-defined integration checks. No new repository defect was evidenced by the current-head CI.

## Vercel and release evidence

- Connected Vercel team discovery succeeded for CalCome team `team_cuJFcIPj1zvkSmGeDk3hckZd` on Hobby, confirming that the connector itself is available.
- Direct deployment listing for project `prj_nNXkUJGK9l7vG83KDZE2XOVHHd80` returned `403 Forbidden` for deployment-list permission.
- Current GitHub Vercel-bot evidence on PR #290 identifies the same project and reports deployment `HSNfasEmzx7XhHAqkByGiTe1oFoV` as **Ready**, with the P-061 Preview URL.
- Retrying that exact deployment ID through the connected Vercel resource path returned `404 Deployment not found`.
- Fetching the protected Preview through the Vercel authenticated-fetch path could not create a shareable access route.

These are resource-visibility/access limitations, not evidence of an application build failure. The GitHub-integrated Preview for the validated P-061 head is Ready.

## Public web observation

Public web retrieval confirms that `https://www.calcome.com/` is reachable and serves the CalCome home surface. Search-engine/web retrieval also finds current canonical Korean and English calculator pages. However, the available directory crawl snapshot was crawled before the latest expansion batch and exposes older category/count data, so it is not used as proof of the current 72-calculator production count.

The newly merged P-061 production route was not discoverable through the available fresh public-search index during this gate. That is consistent with crawl/deployment observation lag and is not, by itself, evidence of a broken route because the exact-head build, route integration tests, and Ready Preview passed.

## Search Console

No connected Search Console data source is available in this execution environment. Therefore no fresh Search Console performance/indexing claim is made. Existing historical Search Console evidence remains background only and does not block this gate.

## Decision

REG-EXP-001 passes. No verified security, calculation, locale, sitemap/indexability, public-route, or critical UX blocker was found in the evidence available to this run. External Production/Preview observation limitations are recorded rather than converted into a false product failure.

Advance autonomous development to P-064. After the next four newly merged calculators, insert the next expansion regression gate before continuing the following batch.
